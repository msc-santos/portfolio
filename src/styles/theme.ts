import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
    palette: {
        primary: {
            main: "#0d1117"
        },
        secondary: {
            main: "#7356b5"
        }
    }
});

export const themeColors = {
    palette: {
        primary: '#0d1117',
        secondary: '#161b22',
        white: '#ffffff'
    }
}